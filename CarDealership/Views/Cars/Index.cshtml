@using CarDealership.Models;

@if (Model.Count == 0)
{
    <p>There are no cars available currently.</p>
}
<ul>
    @foreach (Car car in Model)
    {
        <li>@car.MakeModel</li>
    }
</ul>
<a href="/cars/new">Add a new car </a>
<a href="/search/new">Search for a car</a>